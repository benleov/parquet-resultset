version '1.0-SNAPSHOT'

apply plugin: 'java'

sourceCompatibility = 1.8

repositories {
    mavenCentral()
    jcenter()
}

dependencies {
    implementation  group: 'org.apache.parquet', name: 'parquet-avro', version: '1.8.1'
    implementation  group: 'org.apache.hadoop', name: 'hadoop-common', version: '2.7.3'

    testImplementation  group: 'junit', name: 'junit', version: '4.12'
    testImplementation  group: 'org.apache.commons', name: 'commons-io', version: '1.3.2'
    testImplementation  group: 'org.mockito', name: 'mockito-core', version: '2.+'
}
